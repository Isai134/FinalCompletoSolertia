name: Warm up Render

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping health
        run: |
          curl -fsS https://solertia.onrender.com/health >/dev/null
          curl -fsS https://solertia-kifu.onrender.com >dev/null
